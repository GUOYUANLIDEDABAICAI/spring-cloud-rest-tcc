server:
  port: 8040
management:
  port: 10912
  context-path: '/78d504ff-82e8-4a87-82e8-724d72d1171b'
eureka:
  client:
    service-url:
      defaultZone: 'http://localhost:8763/eureka/,http://localhost:8762/eureka/'
  instance:
    metadata-map:
      cluster: prontera
      management:
        port: ${management.port}
        context-path: ${management.context-path}
      health:
        path: ${management.context-path}/health
    status-page-url-path: ${management.context-path}/info
    health-check-url-path: ${management.context-path}/health
    prefer-ip-address: false
spring:
  application:
    name: 'product'
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://139.199.197.45:3308/product?useUnicode=true&characterEncoding=utf8&useSSL=false&tinyInt1isBit=false
  rabbitmq:
    host: 139.199.197.45
    username: guest
    password: guest
    port: 5672
#    virtual-host: prontera
#    listener:
#      retry:
#        enabled: true
#    template:
#      retry:
#        enabled: true
#      mandatory: true
#    publisher-returns: true
#    publisher-confirms: true
  cloud:
    loadbalancer:
      retry:
        enabled: true
    config:
      discovery:
        enabled: true
        service-id: 'config-server'
      fail-fast: true
  zipkin:
    base-url: 'http://localhost:9411/'
encrypt:
  key: '*&3loa#@9no_0*(!'
mybatis:
  type-handlers-package: 'com.github.prontera.domain.type.handler'
  mapper-locations: 'classpath*:mapper/**/*Mapper.xml'
  type-aliases-package: 'com.github.prontera.domain'
flyway:
  enabled: true
  baseline-on-migrate: true
  validate-on-migrate: false
